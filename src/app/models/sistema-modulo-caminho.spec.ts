import { SistemaModuloCaminho } from './sistema-modulo-caminho';

describe('SistemaModuloCaminho', () => {
  it('should create an instance', () => {
    expect(new SistemaModuloCaminho()).toBeTruthy();
  });
});
